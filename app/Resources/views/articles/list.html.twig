{% extends 'articles/base.html.twig' %}

{% block body %}
    <div class="header">
        <h3>List of articles</h3>
    </div>
    <div class="content">
        <div class="count">
            <h4> <u> Total amount of aritcles: {{ results.getTotalItemCount }} </u></h4>
        </div>
        <table>
            <tr>
                <th>ID</th>
                <th>TITLE</th>
            </tr>
            {% for result in results %}
                <tr>
                    <td>{{ result.id }}</td>
                    <td>{{ result.title }}</td>
                    <td><a href="{{ result.id }}">Read</a></td>
                    <td><a href="x">Edit</a></td>
                    <td><a href="y">Delete</a></td>
                </tr>
            {% endfor %}
        </table>
        <div class="navigation" style="text-align:center">
            {{ knp_pagination_render(results) }}
        </div>
    </div>


{% endblock %}